# Optional. Only needed for registry (network) commands: pin, audit, and the
# registry/audit planes of check. Everything else works without it.
# Value: a bearer token accepted by skills.sh — today that is a Vercel OIDC token.
# See README > Authentication for how to obtain one (vercel env pull).
SKILLS_SH_API_KEY=
